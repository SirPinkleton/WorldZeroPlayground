Fix seed.py guard + script_utils URL normalization for Render

- seed.py: replace hard-abort guard with per-component checks so the
  script can seed tasks even when the pixie account already exists;
  also removed broken incomplete submissions/votes block
- script_utils.py: normalize postgres:// and postgresql:// to
  postgresql+asyncpg:// so scripts work with Render's DATABASE_URL
  without manual conversion (mirrors what start.sh already does)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
