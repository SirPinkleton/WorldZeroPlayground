/**
 * Everymen full-page backdrop — union poster wall (manila stock, propaganda
 * sunburst, screen-print halftone, burlap weave). Theme-aware via the
 * `.em-backdrop` rule in index.css. Fixed behind page content at z-index 0.
 */
export default function EverymenBackdrop() {
  return <div className="em-backdrop" aria-hidden="true" />
}
